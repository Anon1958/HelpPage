"C:\Users\iams395\Copy of Project Tracking.xlsx"

I need you to enhance the existing "Recurring" tab in this workbook. Do NOT touch the "Recurring Schedule" tab -- that's being deprecated in favor of improving the main "Recurring" tab.

Here is what the Recurring tab currently has: rows = individual deliverables, columns = Name, Frequency, Timing, Owner, GV Day, then monthly date columns across the top.

I need you to ADD the following columns to the right of the existing data for each deliverable row. Add them in this exact order:

1. **Preparer** -- who prepares this report
2. **Reviewed By** -- who reviews it  
3. **Distributed** -- date or checkbox when distributed
4. **Due Date (Month)** -- this should be the LAST column, showing the expected due date for the current month based on GV Day and the current month's calendar

Then apply conditional formatting logic as follows:
- If today's date is PAST the GV Day for the current month and the Distributed column is blank → fill RED (overdue, not distributed)
- If today's date is PAST the GV Day but Distributed is filled → fill GREEN (complete)
- If today's date is within 3 days of the GV Day and Distributed is blank → fill YELLOW (due soon)
- If the due date hasn't arrived yet → no fill (white/default)

Also add a summary row or sidebar that flags any RED rows with the label "OVERDUE" so the boss can see at a glance which reports are behind.

Preserve all existing data, formatting, tab structure, and formulas. Only add new columns and conditional formatting to the Recurring tab. Save the file when done.
